header.header
  .header__column
    a(href=globalRoutes.HOME)
      i.fab.fa-youtube
  .header__column
    form(action=globalRoutes.SEARCH, method="get")
      input(type="text", placeholder="Search by term...", name="term")
  .header__column
    ul
      li
        a(href=globalRoutes.JOIN) Join
      li
        a(href=globalRoutes.LOGIN) Log in