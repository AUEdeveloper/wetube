mixin videoBlock(video = {})
  .videoBlock
    a(href=videosRoutes.VIDEO_DETAILS(video.id))
      video.videoBlock__thumbnail(src=video.fileUrl, controls=true)
      h4.videoBlock__title=video.title
      h6.videoBlock__views=video.views